{
  "name": "xr-messaging-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
     "prestart": "mkdir -p ./public && cp ../frontend/scribe-cockpit.html ./public/",
    "start": "node server.js"
  },
  "dependencies": {
    "@socket.io/redis-adapter": "^8.3.0",
    "dotenv": "^17.2.1",
    "express": "^4.19.2",
    "redis": "^5.8.0",
    "socket.io": "^4.7.2"
  }
}
